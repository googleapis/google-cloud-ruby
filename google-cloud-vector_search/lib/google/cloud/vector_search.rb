# frozen_string_literal: true

# Copyright 2026 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# Auto-generated by gapic-generator-ruby. DO NOT EDIT!

# Require this file early so that the version constant gets defined before
# requiring "google/cloud". This is because google-cloud-core will load the
# entrypoint (gem name) file, which in turn re-requires this file (hence
# causing a require cycle) unless the version constant is already defined.
require "google/cloud/vector_search/version"

require "googleauth"
gem "google-cloud-core"
require "google/cloud" unless defined? ::Google::Cloud.new
require "google/cloud/config"

# Set the default configuration
::Google::Cloud.configure.add_config! :vector_search do |config|
  config.add_field! :endpoint,      nil, match: ::String
  config.add_field! :credentials,   nil, match: [::String, ::Hash, ::Google::Auth::Credentials]
  config.add_field! :scope,         nil, match: [::Array, ::String]
  config.add_field! :lib_name,      nil, match: ::String
  config.add_field! :lib_version,   nil, match: ::String
  config.add_field! :interceptors,  nil, match: ::Array
  config.add_field! :timeout,       nil, match: ::Numeric
  config.add_field! :metadata,      nil, match: ::Hash
  config.add_field! :retry_policy,  nil, match: [::Hash, ::Proc]
  config.add_field! :quota_project, nil, match: ::String
  config.add_field! :universe_domain, nil, match: ::String
end

module Google
  module Cloud
    module VectorSearch
      ##
      # Create a new client object for DataObjectSearchService.
      #
      # By default, this returns an instance of
      # [Google::Cloud::VectorSearch::V1::DataObjectSearchService::Client](https://cloud.google.com/ruby/docs/reference/google-cloud-vector_search-v1/latest/Google-Cloud-VectorSearch-V1-DataObjectSearchService-Client)
      # for a gRPC client for version V1 of the API.
      # However, you can specify a different API version by passing it in the
      # `version` parameter. If the DataObjectSearchService service is
      # supported by that API version, and the corresponding gem is available, the
      # appropriate versioned client will be returned.
      # You can also specify a different transport by passing `:rest` or `:grpc` in
      # the `transport` parameter.
      #
      # Raises an exception if the currently installed versioned client gem for the
      # given API version does not support the given transport of the DataObjectSearchService service.
      # You can determine whether the method will succeed by calling
      # {Google::Cloud::VectorSearch.data_object_search_service_available?}.
      #
      # ## About DataObjectSearchService
      #
      # Service for searching data objects.
      #
      # @param version [::String, ::Symbol] The API version to connect to. Optional.
      #   Defaults to `:v1`.
      # @param transport [:grpc, :rest] The transport to use. Defaults to `:grpc`.
      # @return [::Object] A client object for the specified version.
      #
      def self.data_object_search_service version: :v1, transport: :grpc, &block
        require "google/cloud/vector_search/#{version.to_s.downcase}"

        package_name = Google::Cloud::VectorSearch
                       .constants
                       .select { |sym| sym.to_s.downcase == version.to_s.downcase.tr("_", "") }
                       .first
        service_module = Google::Cloud::VectorSearch.const_get(package_name).const_get(:DataObjectSearchService)
        service_module = service_module.const_get(:Rest) if transport == :rest
        service_module.const_get(:Client).new(&block)
      end

      ##
      # Determines whether the DataObjectSearchService service is supported by the current client.
      # If true, you can retrieve a client object by calling {Google::Cloud::VectorSearch.data_object_search_service}.
      # If false, that method will raise an exception. This could happen if the given
      # API version does not exist or does not support the DataObjectSearchService service,
      # or if the versioned client gem needs an update to support the DataObjectSearchService service.
      #
      # @param version [::String, ::Symbol] The API version to connect to. Optional.
      #   Defaults to `:v1`.
      # @param transport [:grpc, :rest] The transport to use. Defaults to `:grpc`.
      # @return [boolean] Whether the service is available.
      #
      def self.data_object_search_service_available? version: :v1, transport: :grpc
        require "google/cloud/vector_search/#{version.to_s.downcase}"
        package_name = Google::Cloud::VectorSearch
                       .constants
                       .select { |sym| sym.to_s.downcase == version.to_s.downcase.tr("_", "") }
                       .first
        return false unless package_name
        service_module = Google::Cloud::VectorSearch.const_get package_name
        return false unless service_module.const_defined? :DataObjectSearchService
        service_module = service_module.const_get :DataObjectSearchService
        if transport == :rest
          return false unless service_module.const_defined? :Rest
          service_module = service_module.const_get :Rest
        end
        service_module.const_defined? :Client
      rescue ::LoadError
        false
      end

      ##
      # Create a new client object for DataObjectService.
      #
      # By default, this returns an instance of
      # [Google::Cloud::VectorSearch::V1::DataObjectService::Client](https://cloud.google.com/ruby/docs/reference/google-cloud-vector_search-v1/latest/Google-Cloud-VectorSearch-V1-DataObjectService-Client)
      # for a gRPC client for version V1 of the API.
      # However, you can specify a different API version by passing it in the
      # `version` parameter. If the DataObjectService service is
      # supported by that API version, and the corresponding gem is available, the
      # appropriate versioned client will be returned.
      # You can also specify a different transport by passing `:rest` or `:grpc` in
      # the `transport` parameter.
      #
      # Raises an exception if the currently installed versioned client gem for the
      # given API version does not support the given transport of the DataObjectService service.
      # You can determine whether the method will succeed by calling
      # {Google::Cloud::VectorSearch.data_object_service_available?}.
      #
      # ## About DataObjectService
      #
      # Service for creating and managing data objects.
      #
      # @param version [::String, ::Symbol] The API version to connect to. Optional.
      #   Defaults to `:v1`.
      # @param transport [:grpc, :rest] The transport to use. Defaults to `:grpc`.
      # @return [::Object] A client object for the specified version.
      #
      def self.data_object_service version: :v1, transport: :grpc, &block
        require "google/cloud/vector_search/#{version.to_s.downcase}"

        package_name = Google::Cloud::VectorSearch
                       .constants
                       .select { |sym| sym.to_s.downcase == version.to_s.downcase.tr("_", "") }
                       .first
        service_module = Google::Cloud::VectorSearch.const_get(package_name).const_get(:DataObjectService)
        service_module = service_module.const_get(:Rest) if transport == :rest
        service_module.const_get(:Client).new(&block)
      end

      ##
      # Determines whether the DataObjectService service is supported by the current client.
      # If true, you can retrieve a client object by calling {Google::Cloud::VectorSearch.data_object_service}.
      # If false, that method will raise an exception. This could happen if the given
      # API version does not exist or does not support the DataObjectService service,
      # or if the versioned client gem needs an update to support the DataObjectService service.
      #
      # @param version [::String, ::Symbol] The API version to connect to. Optional.
      #   Defaults to `:v1`.
      # @param transport [:grpc, :rest] The transport to use. Defaults to `:grpc`.
      # @return [boolean] Whether the service is available.
      #
      def self.data_object_service_available? version: :v1, transport: :grpc
        require "google/cloud/vector_search/#{version.to_s.downcase}"
        package_name = Google::Cloud::VectorSearch
                       .constants
                       .select { |sym| sym.to_s.downcase == version.to_s.downcase.tr("_", "") }
                       .first
        return false unless package_name
        service_module = Google::Cloud::VectorSearch.const_get package_name
        return false unless service_module.const_defined? :DataObjectService
        service_module = service_module.const_get :DataObjectService
        if transport == :rest
          return false unless service_module.const_defined? :Rest
          service_module = service_module.const_get :Rest
        end
        service_module.const_defined? :Client
      rescue ::LoadError
        false
      end

      ##
      # Create a new client object for VectorSearchService.
      #
      # By default, this returns an instance of
      # [Google::Cloud::VectorSearch::V1::VectorSearchService::Client](https://cloud.google.com/ruby/docs/reference/google-cloud-vector_search-v1/latest/Google-Cloud-VectorSearch-V1-VectorSearchService-Client)
      # for a gRPC client for version V1 of the API.
      # However, you can specify a different API version by passing it in the
      # `version` parameter. If the VectorSearchService service is
      # supported by that API version, and the corresponding gem is available, the
      # appropriate versioned client will be returned.
      # You can also specify a different transport by passing `:rest` or `:grpc` in
      # the `transport` parameter.
      #
      # Raises an exception if the currently installed versioned client gem for the
      # given API version does not support the given transport of the VectorSearchService service.
      # You can determine whether the method will succeed by calling
      # {Google::Cloud::VectorSearch.vector_search_service_available?}.
      #
      # ## About VectorSearchService
      #
      # VectorSearchService provides methods for managing Collection resources, and
      # Collection Index resources. The primary resources offered by this service are
      # Collections which are a container for a set of related JSON data objects, and
      # Collection Indexes which enable efficient ANN search across data objects
      # within a Collection.
      #
      # @param version [::String, ::Symbol] The API version to connect to. Optional.
      #   Defaults to `:v1`.
      # @param transport [:grpc, :rest] The transport to use. Defaults to `:grpc`.
      # @return [::Object] A client object for the specified version.
      #
      def self.vector_search_service version: :v1, transport: :grpc, &block
        require "google/cloud/vector_search/#{version.to_s.downcase}"

        package_name = Google::Cloud::VectorSearch
                       .constants
                       .select { |sym| sym.to_s.downcase == version.to_s.downcase.tr("_", "") }
                       .first
        service_module = Google::Cloud::VectorSearch.const_get(package_name).const_get(:VectorSearchService)
        service_module = service_module.const_get(:Rest) if transport == :rest
        service_module.const_get(:Client).new(&block)
      end

      ##
      # Determines whether the VectorSearchService service is supported by the current client.
      # If true, you can retrieve a client object by calling {Google::Cloud::VectorSearch.vector_search_service}.
      # If false, that method will raise an exception. This could happen if the given
      # API version does not exist or does not support the VectorSearchService service,
      # or if the versioned client gem needs an update to support the VectorSearchService service.
      #
      # @param version [::String, ::Symbol] The API version to connect to. Optional.
      #   Defaults to `:v1`.
      # @param transport [:grpc, :rest] The transport to use. Defaults to `:grpc`.
      # @return [boolean] Whether the service is available.
      #
      def self.vector_search_service_available? version: :v1, transport: :grpc
        require "google/cloud/vector_search/#{version.to_s.downcase}"
        package_name = Google::Cloud::VectorSearch
                       .constants
                       .select { |sym| sym.to_s.downcase == version.to_s.downcase.tr("_", "") }
                       .first
        return false unless package_name
        service_module = Google::Cloud::VectorSearch.const_get package_name
        return false unless service_module.const_defined? :VectorSearchService
        service_module = service_module.const_get :VectorSearchService
        if transport == :rest
          return false unless service_module.const_defined? :Rest
          service_module = service_module.const_get :Rest
        end
        service_module.const_defined? :Client
      rescue ::LoadError
        false
      end

      ##
      # Configure the google-cloud-vector_search library.
      #
      # The following configuration parameters are supported:
      #
      # * `credentials` (*type:* `String, Hash, Google::Auth::Credentials`) -
      #   The path to the keyfile as a String, the contents of the keyfile as a
      #   Hash, or a Google::Auth::Credentials object.
      # * `lib_name` (*type:* `String`) -
      #   The library name as recorded in instrumentation and logging.
      # * `lib_version` (*type:* `String`) -
      #   The library version as recorded in instrumentation and logging.
      # * `interceptors` (*type:* `Array<GRPC::ClientInterceptor>`) -
      #   An array of interceptors that are run before calls are executed.
      # * `timeout` (*type:* `Numeric`) -
      #   Default timeout in seconds.
      # * `metadata` (*type:* `Hash{Symbol=>String}`) -
      #   Additional headers to be sent with the call.
      # * `retry_policy` (*type:* `Hash`) -
      #   The retry policy. The value is a hash with the following keys:
      #     * `:initial_delay` (*type:* `Numeric`) - The initial delay in seconds.
      #     * `:max_delay` (*type:* `Numeric`) - The max delay in seconds.
      #     * `:multiplier` (*type:* `Numeric`) - The incremental backoff multiplier.
      #     * `:jitter` (*type:* `Numeric`) - The jitter in seconds. Default: 1.0.
      #     * `:retry_codes` (*type:* `Array<String>`) -
      #       The error codes that should trigger a retry.
      #
      # @return [::Google::Cloud::Config] The default configuration used by this library
      #
      def self.configure
        yield ::Google::Cloud.configure.vector_search if block_given?

        ::Google::Cloud.configure.vector_search
      end
    end
  end
end

helper_path = ::File.join __dir__, "vector_search", "helpers.rb"
require "google/cloud/vector_search/helpers" if ::File.file? helper_path
